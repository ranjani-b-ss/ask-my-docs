"""Week 7 — a hand-built ReAct agent, a fixed workflow doing the identical task, and the
tools both of them share.

Nothing here imports a framework. The whole point of the week is that the loop is small
enough to read in one sitting: see react_agent.py.
"""
